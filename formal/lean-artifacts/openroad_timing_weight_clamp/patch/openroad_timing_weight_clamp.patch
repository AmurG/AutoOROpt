diff --git a/src/gpl/src/timingBase.cpp b/src/gpl/src/timingBase.cpp
index 976ce4ebe..b9be42e82 100644
--- a/src/gpl/src/timingBase.cpp
+++ b/src/gpl/src/timingBase.cpp
@@ -114,7 +114,8 @@ size_t TimingBase::getTimingNetWeightOverflowSize() const
 
 void TimingBase::setTimingNetWeightMax(float max)
 {
-  net_weight_max_ = max;
+  // Ensure we never request a "maximum" below the default weight (1.0).
+  net_weight_max_ = std::max(1.0f, max);
 }
 
 bool TimingBase::executeTimingDriven(bool run_journal_restore)
@@ -167,9 +168,11 @@ bool TimingBase::executeTimingDriven(bool run_journal_restore)
         } else {
           // weight(min_slack) = net_weight_max_
           // weight(max_slack) = 1
-          const float weight = 1
-                               + (net_weight_max_ - 1) * (slack_max - net_slack)
-                                     / (slack_max - slack_min);
+          const float raw_weight = 1
+                                   + (net_weight_max_ - 1)
+                                         * (slack_max - net_slack)
+                                         / (slack_max - slack_min);
+          const float weight = std::clamp(raw_weight, 1.0f, net_weight_max_);
           gNet->setTimingWeight(weight);
         }
         weighted_net_count++;
